import 'dart:ui';

/// These will form the Colors for the theme.
/// The colors defined here are only available via JobFinderThemeColors.
/// With this we will have a constant use of the Theme colors directly.
class JFThemeColors {
  JFThemeColors._();
  /*Primary Colors*/
  static const Color black = Color.fromRGBO(9, 9, 9, 0.7);
  static const Color white = Color.fromRGBO(255, 255, 255, 0.7);
/*Shades of Green*/
  static const Color limeGreen = Color.fromRGBO(50, 205, 50, 1.0);
  static const Color lightLimeGreen = Color.fromRGBO(74, 187, 0, 1.0);
  static const Color splashLimeGreen = Color.fromRGBO(174, 247, 136, 0.8);
/*Shades of Blue*/
  static const Color bluish = Color.fromRGBO(89, 68, 190, 0.7019607843137254);
  static const Color darkBlue = Color.fromRGBO(46, 36, 86, 1.0);
  /*Shades of REd*/
  static const Color reddish = Color.fromRGBO(255, 67, 67, 1.0);
  static const Color warmRed = Color.fromRGBO(255, 119, 84, 1.0);
  static const Color buttonRed = Color.fromRGBO(255, 119, 119, 0.7);

  /*Shades of GraY*/
  static const Color grey = Color.fromRGBO(239, 239, 239, 1.0);
  static const Color lightGrey = Color.fromRGBO(246, 246, 246, 1.0);
  static const Color deepGray = Color.fromRGBO(230, 228, 230, 1.0);
  static const Color darkGray = Color.fromRGBO(131, 130, 154, 1.0);
  static const Color darkerGray = Color.fromRGBO(164, 164, 166, 1.0);


}